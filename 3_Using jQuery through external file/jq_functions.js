$(document).ready(function () {       // We are using Nesting Query method
			$("button").click(function () {  // Syntax  $(documnt).ready(function () {.. jQuery methods.. }); 
				$("p").hide();
			});
		});